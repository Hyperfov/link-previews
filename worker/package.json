{
  "private": true,
  "name": "link-preview-worker",
  "version": "1.0.0",
  "description": "A worker that retrieves and serves a page's meta information",
  "main": "index.ts",
  "scripts": {
    "dev": "npx wrangler dev",
    "publish": "npx wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Christian Broms <cb@christianbroms.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "prettier": "^1.18.2",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "wrangler": "^2.1.15"
  },
  "dependencies": {
    "node-html-parser": "^4.1.5"
  }
}
